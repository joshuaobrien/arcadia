services:
  arcadia-test:
    command: ./test-environment/bin/orb-stack
    port: 8787
    healthcheck:
      path: /api/health
    portal:
      title: Arcadia test environment
      description: Production-faithful seeded music stack
